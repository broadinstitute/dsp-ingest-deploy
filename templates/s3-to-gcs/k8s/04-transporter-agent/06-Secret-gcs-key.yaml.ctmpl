{{with $secretPrefix := env "VAULT_PREFIX"}}
{{with $secret := secret (printf "%s/transporter/gcs-writer-sa-key" $secretPrefix)}}
apiVersion: v1
kind: Secret
metadata:
  name: transporter-gcs-sa-key
type: Opaque
data:
  json-key: {{$secret.Data | toJSON | base64Encode}}
{{end}}{{end}}
